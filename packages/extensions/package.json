{
  "name": "@yukako/extensions",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
	"build": "node scripts/worker_build.mjs",
	"dev": "nodemon --watch workers --watch extensions --watch modules --watch scripts --watch ../../node_modules --ext js,ts,jsx,tsx,mjs,mts,json --exec \"npm run build\"",
	"typecheck": "tsc --noEmit"
  },
  "devDependencies": {
	"typescript": "latest"
  }
}
